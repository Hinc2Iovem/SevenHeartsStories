type TranslationChangeLanguageTypes = {
  currentLanguage: string | undefined;
};

export const translationServiceChangeLanguage = async ({
  currentLanguage,
}: TranslationChangeLanguageTypes) => {
  if (currentLanguage?.trim().length) {
  }
  return null;
};
